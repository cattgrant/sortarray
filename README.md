# Prompts the user to enter the number of strings to sort, then reads a sequence of

# strings entered by the user, sorts them in alphabetical ascending order,

# and prints the sorted array of strings.
